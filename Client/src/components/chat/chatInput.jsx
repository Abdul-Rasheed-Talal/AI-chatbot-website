/*
  File: ChatInput.jsx
  Purpose:
    - Input component for the chat page.
    - Allows user to type a message and send it.
  Developer Notes:
    - Accepts props like value, onChange, and onSend callback.
    - Use inside ChatBox component.
    - Only handles UI for typing; actual sending logic is handled in parent (ChatBox or ChatPage).
*/
